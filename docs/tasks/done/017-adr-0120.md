# Task 017: ADR-0120 — state the asset guarantee the code will enforce

- **Mission:** SITE-V1 — **Status:** done (2026-09-03)
- **Agent / model:** manager (direct — ADR text, decision made by Ludwig 2026-09-03)
- **Budget:** small · **Branch:** task/017-adr-0120-content-whitelist

## Objective
The decision log states the guarantee the scanner will actually enforce, so that spec, code and
decision agree. ADR-0004's "scans by magic bytes" wording was satisfied by two demonstrated files
that each carried a byte-for-byte Blizzard file inside a valid container.

## Acceptance criteria
1. ADR-0120 exists, carries `Amends: ADR-0004`, and states the positive guarantee: an accepted
   asset contains only explicitly permitted content. Demonstrated: file read.
2. ADR-0004 carries the `Amended by: ADR-0120` back-reference the log's rules require.
3. Indexed, and the INDEX.json gate passes with 120 ADRs.
4. Records why CRC verification is *not* the fix — an attacker computes the correct CRC — so the
   reviewer's suggestion is not silently revived later.
5. Records option B (re-encode on ingest) as the available future hardening, not as rejected.

## Log
- 2026-09-03 written, indexed, gate green at 120 ADRs, merged by PR.
